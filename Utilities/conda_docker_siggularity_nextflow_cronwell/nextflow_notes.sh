# 核心诊断：这验证了“10% 比对率”的什么？
# 证据确凿：你看 Command 列，它们都在运行 --ref rfam-5.8s-database...。
# 运行时间：注意 TIME+ 列。第一个进程已经跑了 8 小时 05 分钟！
# 分析：一个普通的、rRNA 除得很干净的 RNA-seq 样本，跑 SortMeRNA 通常只需要 10-20 分钟。你的样本跑了 8 小时还没跑完，这百分之百证明了你的样本里全是 rRNA。 软件正在从几亿条垃圾序列里艰难地“淘金”

# 不要杀掉进程：现在杀掉会前功尽弃。
# 调整 Nextflow 的并发（下次优化）：
# 目前的 Nextflow 似乎限制了只有 10 个任务同时跑。既然你内存充足，下次你可以设置 --max_forks 20。
# 检查硬盘 IO：
# 你可以输入 
iostat -x 1 #（如果没有请安装 sysstat :sudo apt install sysstat）。如果 %util 接近 100%，说明你的硬盘已经冒烟了，这就是为什么 CPU 跑不动的原因。